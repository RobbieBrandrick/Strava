    <template id="activity-charts">
        <div class="row">
    
            <div class="col-xl-6">
                <cardio-chart type="Run" :group-data="runningState.groupByDate"></cardio-chart>
            </div>
            <div class="col-xl-6">
                <cardio-chart type="Ride" :group-data="runningState.groupByDate"></cardio-chart>
            </div>
    
        </div>
    </template>
    
    <script>
        Vue.component("activity-charts", {
            template: "#activity-charts",
            data(){
                return {
                    runningState: runningActivityStore
                };            
            }
        });
    </script>